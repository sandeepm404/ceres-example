// Watermark widget — pure CSS side-effect.
// Importing this file bundles watermark/styles.css into the template's CSS bundle.
// No Handlebars partial needed; the watermark is a CSS overlay driven by
// CSS variables set by applyPreviewStyles() in commonUtils.ts.
import "./styles.css";
